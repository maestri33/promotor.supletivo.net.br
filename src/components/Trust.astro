---
/**
 * Seção 4 — Confiança. Construída só com a mecânica REAL (sem prova social
 * inventada): Pix automático, custo zero pra entrar, chave validada no banco,
 * login sem senha. Bloco de marcas do polo (HUB_BRANDS) fica gated: só aparece
 * quando preenchido — decisão jurídica/comercial.
 */
import Marquee from './Marquee.astro';
import BadgePromotorOficial from './badges/BadgePromotorOficial.astro';
import BadgePixGarantido from './badges/BadgePixGarantido.astro';
import BadgeValidadeOficial from './badges/BadgeValidadeOficial.astro';
import { CLOSING_LABEL, HUB_BRANDS } from '../config';

const seals = [
  `Pagamento por Pix, ${CLOSING_LABEL}, automático`,
  'Grátis pra entrar — você nunca paga nada',
  'Sua chave Pix é validada no banco (DICT)',
];

const objections = [
  {
    q: 'Tem pegadinha?',
    a: 'Não. Você nunca paga para ser promotor. Quem paga é quem se matricula — e é isso que gera a sua comissão.',
  },
  {
    q: 'É seguro receber por aqui?',
    a: 'Sim. A chave Pix é validada direto no banco e precisa estar no seu CPF. Documento e selfie confirmam que é você.',
  },
  {
    q: 'Vou ter que vender?',
    a: 'Não. Você só compartilha o seu link. Sem abordagem, sem meta obrigatória, sem estoque.',
  },
];
---

<section id="confianca" class="trust section on-dark grain" aria-labelledby="trust-title" data-section="confianca">
  <div class="trust-band">
    <Marquee
      items={['Pix toda semana', 'Grátis pra entrar', 'Sem vender nada', 'Validação no banco']}
      variant="green"
      speed={34}
      tilt={-2.5}
    />
  </div>
  <span class="sec-num" aria-hidden="true" data-n="04"></span>
  <svg class="trust-shield" viewBox="0 0 100 116" fill="none" aria-hidden="true">
    <path
      d="M50 4l40 14v34c0 26-17 45-40 60C27 97 10 78 10 52V18z"
      stroke="currentColor"
      stroke-width="2.5"
      stroke-linejoin="round"></path>
    <path
      class="shield-check"
      pathLength="1"
      d="M32 56l12 12 24-26"
      stroke="currentColor"
      stroke-width="5"
      stroke-linecap="round"
      stroke-linejoin="round"></path>
  </svg>
  <div class="container">
    <p class="kicker trust-kicker" data-reveal>Por que confiar</p>
    <h2 id="trust-title" data-reveal>Dinheiro de verdade, na conta certa.</h2>
    <p class="trust-lead" data-reveal style="--d: 1">
      Sem letra miúda: você não investe nada para começar e recebe direto no seu
      Pix. O cadastro é sério só pra garantir que o dinheiro vai pra pessoa certa.
    </p>

    <!-- Grid de Selos e Verificações Oficiais -->
    <div class="official-badges-grid spatial-stage" data-reveal style="--d: 2">
      <div class="official-badge-card glass-panel-gold spatial-card-3d" data-tilt-3d data-tilt-max="5">
        <BadgePromotorOficial size={76} showText={true} />
      </div>
      <div class="official-badge-card glass-panel-gold spatial-card-3d" data-tilt-3d data-tilt-max="5">
        <BadgePixGarantido size={76} showText={true} />
      </div>
      <div class="official-badge-card glass-panel-gold spatial-card-3d" data-tilt-3d data-tilt-max="5">
        <BadgeValidadeOficial size={76} showText={true} />
      </div>
    </div>

    <ul class="seals spatial-stage">
      {
        seals.map((seal, i) => (
          <li class="seal spatial-card-3d glass-panel" data-seal data-tilt-3d data-tilt-max="8" style={`--d: ${i}`}>
            <svg class="seal-badge layer-z-sm" viewBox="0 0 96 96" fill="none" aria-hidden="true">
              <circle cx="48" cy="48" r="44" stroke="var(--yellow)" stroke-width="2.5" />
              <circle
                cx="48"
                cy="48"
                r="36.5"
                stroke="var(--yellow)"
                stroke-width="1.6"
                stroke-dasharray="3 7"
                stroke-linecap="round"
              />
              <path
                class="seal-check"
                d="M34 49.5l9.5 9.5L62 39"
                stroke="#fff"
                stroke-width="5"
                stroke-linecap="round"
                stroke-linejoin="round"
                pathLength="1"
              />
            </svg>
            <span class="layer-z-sm">{seal}</span>
          </li>
        ))
      }
    </ul>

    <dl class="objections">
      {
        objections.map((item, i) => (
          <div data-reveal style={`--d: ${i + 1}`}>
            <dt>{item.q}</dt>
            <dd>{item.a}</dd>
          </div>
        ))
      }
    </dl>

    {HUB_BRANDS.length > 0 && (
      <div class="brands" data-reveal>
        <p class="brands-label">Instituições do polo</p>
        <ul class="brands-list">
          {HUB_BRANDS.map((b) => <li>{b}</li>)}
        </ul>
      </div>
    )}
  </div>
</section>

<style>
  .trust {
    background:
      radial-gradient(44rem 30rem at 88% 4%, rgb(240 212 147 / 0.16), transparent 58%),
      radial-gradient(46rem 34rem at 8% 58%, rgb(168 123 46 / 0.20), transparent 60%),
      radial-gradient(40rem 30rem at 62% 100%, rgb(217 177 90 / 0.14), transparent 60%),
      var(--char);
    color: var(--paper);
    overflow: clip;
    padding-top: calc(var(--space-section) + 2rem);
  }

  .trust .sec-num {
    color: var(--paper);
    top: clamp(4rem, 8vw, 6rem);
  }

  .trust-band {
    position: absolute;
    top: clamp(-1.4rem, -1.5vw, -1rem);
    left: -3%;
    width: 106%;
    z-index: 2;
    box-shadow: 0 14px 30px -16px rgb(0 0 0 / 0.45);
  }

  @media (max-width: 720px) {
    .trust-band :global(.marquee) {
      --tilt: -1.2deg;
    }
  }

  #trust-title {
    font-size: var(--text-h2-sm);
  }

  .trust-shield {
    position: absolute;
    right: clamp(-3rem, -2vw, 0rem);
    top: clamp(5rem, 10vw, 8rem);
    width: clamp(11rem, 20vw, 17rem);
    height: auto;
    color: var(--paper);
    opacity: 0.06;
    pointer-events: none;
    user-select: none;
  }

  @media (prefers-reduced-motion: no-preference) {
    .trust-shield {
      animation: shield-breathe 5.5s ease-in-out infinite;
    }

    .shield-check {
      stroke-dasharray: 1;
      animation: shield-redraw 7s ease-in-out infinite;
    }
  }

  @keyframes shield-breathe {
    50% {
      transform: scale(1.03);
    }
  }

  @keyframes shield-redraw {
    0%,
    8% {
      stroke-dashoffset: 1;
    }
    30%,
    78% {
      stroke-dashoffset: 0;
    }
    94%,
    100% {
      stroke-dashoffset: 1;
    }
  }

  .trust-kicker {
    color: var(--yellow);
  }

  .trust-lead {
    color: var(--muted-on-dark);
    font-size: var(--text-lg);
    max-width: 46ch;
    margin-top: 1.2rem;
  }

  .official-badges-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(17rem, 1fr));
    gap: 1.25rem;
    margin-top: 2.6rem;
  }

  .official-badge-card {
    padding: 1.3rem 1.4rem;
    border-radius: var(--radius-lg);
    display: flex;
    align-items: center;
    transition: transform 0.3s var(--ease-spring), box-shadow 0.3s var(--ease-out);
  }

  .official-badge-card:hover {
    transform: translateY(-4px);
  }

  .seals {
    list-style: none;
    margin: 2.2rem 0 0;
    padding: 0;
    display: grid;
    gap: 1.3rem;
    grid-template-columns: repeat(auto-fit, minmax(15rem, 1fr));
  }

  .seal {
    display: flex;
    align-items: center;
    gap: 1.1rem;
    background: rgb(255 255 255 / 0.06);
    border: 1px solid rgb(255 255 255 / 0.14);
    border-radius: var(--radius);
    padding: 1.2rem 1.3rem;
    font-weight: 700;
    line-height: 1.4;
    font-size: 0.95rem;
    backdrop-filter: blur(4px);
    transition: border-color 0.3s var(--ease-out);
  }

  .seal:hover {
    border-color: rgb(217 177 90 / 0.5);
  }

  @media (min-width: 880px) {
    .seal:nth-child(1) {
      rotate: -1.2deg;
    }

    .seal:nth-child(2) {
      rotate: 0.8deg;
      translate: 0 0.5rem;
    }

    .seal:nth-child(3) {
      rotate: -0.6deg;
    }
  }

  .seal-badge {
    width: 3.6rem;
    height: 3.6rem;
    flex: none;
  }

  @media (prefers-reduced-motion: no-preference) {
    :global(html.js) [data-seal] {
      opacity: 0;
      transform: scale(1.35) rotate(-5deg);
    }

    :global(html.js) [data-seal].in-view {
      animation: stamp 0.55s var(--ease-stamp) forwards;
      animation-delay: calc(var(--d, 0) * 160ms);
    }

    :global(html.js) [data-seal] .seal-check {
      stroke-dasharray: 1;
      stroke-dashoffset: 1;
    }

    :global(html.js) [data-seal].in-view .seal-check {
      transition: stroke-dashoffset 0.45s var(--ease-out);
      transition-delay: calc(var(--d, 0) * 160ms + 0.3s);
      stroke-dashoffset: 0;
    }
  }

  @keyframes stamp {
    60% {
      opacity: 1;
      transform: scale(0.96) rotate(0.6deg);
    }
    100% {
      opacity: 1;
      transform: scale(1) rotate(0deg);
    }
  }

  .objections {
    margin: 3.2rem 0 0;
    display: grid;
    gap: 1.3rem;
    grid-template-columns: repeat(auto-fit, minmax(15rem, 1fr));
  }

  .objections div {
    border-left: 3px solid var(--yellow);
    padding-left: 1.1rem;
  }

  .objections dt {
    font-family: var(--font-display);
    font-size: 1.05rem;
    margin-bottom: 0.45rem;
  }

  .objections dd {
    margin: 0;
    color: var(--muted-on-dark);
    font-size: 0.95rem;
  }

  /* marcas do polo (gated) */
  .brands {
    margin-top: 3rem;
    padding-top: 1.8rem;
    border-top: 1px solid rgb(255 255 255 / 0.12);
  }

  .brands-label {
    font-size: 0.74rem;
    font-weight: 700;
    letter-spacing: 0.14em;
    text-transform: uppercase;
    color: var(--muted-on-dark);
    margin-bottom: 1rem;
  }

  .brands-list {
    list-style: none;
    margin: 0;
    padding: 0;
    display: flex;
    flex-wrap: wrap;
    gap: 0.7rem 1.4rem;
    align-items: center;
  }

  .brands-list li {
    font-family: var(--font-display);
    font-size: 1.05rem;
    color: var(--paper);
    opacity: 0.85;
  }
</style>
